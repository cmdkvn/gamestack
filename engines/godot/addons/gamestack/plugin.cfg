[plugin]

name="gamestack"
description="Localhost state/action server for the gamestack /playtest, /game-feel-audit, and /perf-benchmark skills. Loopback-only HTTP. Same contract as the Unity SDK so the same /playtest scenarios run unchanged."
author="Alliance Game Studio"
version="0.2.0"
script="editor/editor_plugin.gd"
